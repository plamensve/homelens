{
  "name": "homelens-extension",
  "version": "1.0.0",
  "private": true,
  "description": "HomeLens – анализатор на обяви за имоти",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "validate": "node scripts/validate-extension.js",
    "package": "bash scripts/package-extension.sh"
  },
  "engines": {
    "node": ">=20"
  }
}

